{
    "nio/RESTPolling": {
        "Description": "Used as a base class for any block that needs to do polling of a URL. *Not a stand alone block.*",
        "Properties": {
            "retry_interval": {
                "description": "When a url request fails, how long to wait before attempting to try again."
            },
            "queries": {
                "description": "List of queries."
            },
            "retry_limit": {
                "description": "When a url request fails, number of times to attempt a retry before giving up."
            },
            "polling_interval": {
                "description": "How often url is polled. When using more than one query. Each query will be polled at a period equal to the `polling_interval` times the number of queries."
            }
        },
        "Input": {
            "Description": "None"
        },
        "Category": "WebService",
        "Commands": {},
        "Version": "1.0.0",
        "Output": {
            "Description": "Varies depending on implementation of the base class. `_process_response(self, resp)` is to be implemented to return the signals that will be notified."
        }
    }
}